---
# vars file for libvirt
_libvirt_packages:
  default:
    - "@virt"
  Fedora:
    - libvirt
    - qemu

libvirt_packages: "{{ _libvirt_packages[ansible_distribution] | default('default') }}"

libvirt_service: libvirtd
