---
# vars file for libvirt
_libvirt_packages:
  default:
    - "@virt"
  Fedora:
    - libvirt
    - qemu

libvirt_packages: "{{ _libvirt_packages[ansible_distribution] | default(_libvirt_packages['default']) }}"

libvirt_service: libvirtd
