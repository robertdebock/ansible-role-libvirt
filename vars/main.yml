---
# vars file for libvirt
_libvirt_packages:
  default:
    - libvirt
    - qemu
  RedHat:
    - "@virt"

libvirt_packages: "{{ _libvirt_packages[ansible_os_family] | default('default') }}"

libvirt_service: libvirtd
